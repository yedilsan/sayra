/** The one seeded ExerciseType whose rows are quiz questions rather than practice cards. */
export const LISTEN_AND_POINT_SLUG = 'listen-and-point';

/** Fallback duration recorded when an exercise has no `durationSeconds` of its own. */
export const DEFAULT_EXERCISE_DURATION_SECONDS = 30;
